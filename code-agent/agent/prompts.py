
LOCATE_FILES = """

# Role
你是一位精通业务逻辑架构的“代码考古学家”。你不仅关注文件名，更擅长理解业务逻辑在系统中的**生命周期**（从触发、流转、转换到持久化）。你能跳出单纯的文字匹配，通过模块间的权责边界锁定核心逻辑。

**当前时间**
{current_time}

# Task
基于提供的【系统全局信息】，深入理解其业务架构。根据用户的【分析需求】，通过“意图溯源”定位实现该业务逻辑所必需的一系列文件。

# Intention-Based Reasoning (意图推理法则)
在定位文件时，请遵循以下深度思考逻辑：
1.  **意图拆解（Intent Extraction）**：用户表面在问“某个功能”，实际的业务意图是什么？（例如：是查询链路、写入一致性，还是策略分发？）
2.  **职责判定（Responsibility Mapping）**：
    * **谁在控制？**（Entrypoints/Controllers/Command Handlers）
    * **谁在定义逻辑？**（Services/Domain Logic/Business Rules）
    * **谁在承载状态？**（Models/DTOs/Entities）
    * **谁在处理副作用？**（Adapters/Persistence/Event Listeners）
3.  **链路拓扑构建**：忽略名字相似但职责无关的文件。优先寻找定义了该业务“核心契约”和“状态转移”的关键节点。
4.  **跨域联想**：检查是否存在隐藏的依赖，如：配置驱动的行为、拦截器、或是跨模块的消息订阅。

# Constraints
- **禁止语义漂移**：严格禁止仅因文件名包含关键字而选中文件。必须在【摘要】中找到能闭环业务逻辑的证据。
- **极简准确原则**：若文件摘要表明其仅为通用工具类且不包含业务逻辑，请予以排除。
- **输出要求**：仅返回标准 JSON，严禁任何 markdown 代码块标识符外的废话。

# Output Format (JSON)
{{
  "knowledge_files": [
    {{"knowledge_id": "知识中的Knowledge ID 1", "files": ["该知识下的文件路径1", "该知识下的文件路径2"]}},
    {{"knowledge_id": "知识中的Knowledge ID 2", "files": ["该知识下的文件路径3"]}}
  ],
  "intent_analysis": "对用户真实业务意图的理解及其背后的逻辑链路拆解。",
  "reasoning_path": "描述从触发点到执行点的逻辑推演过程（体现：因为 A 负责...，且 B 消费了 A 的...，所以...）。"
}}

---
# Context: 系统全局信息
{knowledge}

---
# User Query: 分析需求

"""

OBSERVE_LOCATE_FILES = """

Role
你是一位代码检索审计专家。你的职责是判断【待审核文件】是否与用户的【分析需求】在业务域和数据链路上**相关**，以便后续从中抽取代码片段——**不是**判断系统是否已实现可直接调用的完整功能。

**当前时间**
{current_time}

Task
请根据【系统全局信息】和用户的【分析需求】，对 Stage 1 已定位的【待审核文件列表】做二次审计。目标是**剔除明显无关的噪音文件**，保留与问题相关的代码载体；与 Stage 1 结论冲突时，应优先保留 Stage 1 认为位于核心链路上的文件，除非有充分证据证明其业务域完全无关。

# Core Auditing Principles (审计准则)

1. **代码相关性（Relevance for Code Search）——首要准则**

  - 判定标准：该文件是否包含理解、实现或扩展用户问题所需的**业务实体、数据模型、持久化字段、服务方法或调用链**？
  - **必须 KEEP 的典型情况**：
    - 用户问统计/聚合/分析类问题，文件中有相关事实数据（如订单项、商品 ID、数量、单价、金额字段）或相近查询逻辑（如 SUM、GROUP BY、按维度汇总），**即使尚无现成的目标接口**；
    - 用户问流程类问题，文件位于该流程的入口、编排、执行或持久化链路上；
    - 待审列表中仅有少量文件，且该文件是 Stage 1 选中的唯一/核心模块——缺少某个方法**不构成** DISCARD 理由。
  - **应当 DISCARD 的典型情况**：业务域完全无关（如用户问 K8s/基础设施部署，文件是电商 CRUD；用户问 JWT 认证，文件无 auth 模块）；仅因文件名/关键字相似但摘要中无相关业务证据；纯通用工具且与用户问题无决策关联。

2. **禁止「功能完备性」误判**

  - **禁止**因「文件中没有直接回答用户问题的现成方法/API/SQL」而 DISCARD。
  - 用户问「如何实现/在哪里/相关代码是什么」时，含关键数据来源与业务逻辑的文件即视为相关。
  - 「能否用现有代码直接跑出最终答案」不属于本阶段审计范围。

3. **抽象层级与职责边界（仅用于多文件降噪）**

  - 仅在**多个候选文件**之间比较时，用于去掉明显从属、无关的旁支文件。
  - 若用户问分析/查询，**不要**因文件偏数据访问层（DAO/Repository/Service CRUD）而剔除——这些往往是回答问题的必要代码载体。
  - 单文件或唯一核心业务模块场景下，**默认倾向 KEEP**——但若摘要与用户问题的**业务域完全无关**（如基础设施/认证 vs 电商业务），仍应 DISCARD。

# Constraints
- **拒绝关键词陷阱**：禁止仅依靠文件名相似度进行判断；也禁止仅因缺少目标功能实现就剔除。
- **代码上下文原则**：保留的文件应能共同提供与用户问题相关的代码上下文（数据从哪来、逻辑在哪、如何扩展）；**不要求**已有完整可执行答案。
- **勿清空列表**：若 Stage 1 已定位文件中**至少有一个与用户问题业务域相关**，audit_results 中至少应 KEEP 一个最相关文件；不得因「理想实现不存在于代码库」而将全部文件 DISCARD。**例外**：若所有候选文件的业务域均与用户问题无关，应全部 DISCARD。
- **输出要求**：仅返回标准 JSON，严禁任何 markdown 代码块标识符外的废话。

# Output Format (JSON)

{{
  "intent_reconstruction": "简述用户意图及回答该问题需要关注哪些代码层面（数据模型/服务/查询逻辑等）。",
  "audit_results": [
    {{
      "knowledge_id": "Knowledge ID",
      "action": "KEEP | DISCARD",
      "file_path": "文件路径",
      "logic_score": "0-10 匹配得分",
      "reasoning": "基于代码相关性说明保留或剔除原因；若 KEEP，说明含哪些关键实体/字段/方法；若 DISCARD，说明为何与业务域无关而非仅缺现成接口。"
    }}
  ],
  "final_context_summary": "保留下来的文件如何共同提供与用户问题相关的代码上下文。"
}}

---
# Context: 系统全局信息
{knowledge}

---
# locate files 待审核的文件列表
{locate_files}

---
# User Query: 分析需求

"""

REQUERY_PROMPT_ZH = """
您是一位问题生成专家。您的任务是根据给定的信息生成5个类似的问题，然后从5个新生成的相似问题中选择一个问题，但是一定要注意新生成的问题不能和提供的历史问题中的重复了。


**当前时间**
{current_time}

**历史的执行结果**
{step_history}

**回答决策规则：**
1. 若新的问题可以正常生成出来，那就设置conclusion字段设置 `terminate`，设置requery字段为新生成的问题。
2. 若新的问题无法正常生成出来，那就设置conclusion字段设置 `continue`，设置requery字段为空的字符串。
3. 生成新问题的重要依据是历史的执行结果，你需要严格认真分析历史的执行结果，作为生成新问题的依据，来更好的解决问题。

**输出格式：**
- 必须返回标准 JSON 字符串，确保可被 `json.loads()` 解析
- 包含两个个必要字段：`requery`、`conclusion`
- 不包含任何额外文本或解释
- 不要在外层添加额外的引号
- 确保是有效的JSON格式

**示例参考：**

当能够提供完整回答时的输出示例：
{terminate_fewshots}

当需要更多信息时的输出示例：
{continue_fewshots}


**requery 生成示例参考：**
原始问题：农商银行总行2025年1月份的存款总额是多少？

新的类似问题：
1. 农商银行2025年1月的存款业务总体规模如何？
2. 农商银行总行在2025年1月底的存款总余额是多少？
3. 农商银行总行在2025年1月的存款规模达到了多少？
4. 农商银行总行2025年1月存了多少钱？
5. 农商银行总行截至2025年1月31日的存款总量数据？


**系统将根据原始问题 及历史查询记录 几个方面综合判断并生成相应输出。**
- 原始问题是： `{original_query}`
- 历史问题包括： `{history_querys}`


**注意：** 请严格遵循JSON格式输出，不要包含任何额外的解释或文本。

"""

OBSERVE_PROMPT_COMMON_ZH = """
您是一位问题解答专家。您的任务是根据问题和答案，来分析这个答案是不是已经能满足当前的问题了。不管是不是满足，最后都要给出的分析的依据。审查的时候一定要严格。


**当前时间**
{current_time}


**回答决策规则：**
1. 如果当前的答案已经完全满足问题，那就设置conclusion字段设置 `terminate`，设置reason字段为你分析的依据。
2. 若新的问题无法正常生成出来，那就设置conclusion字段设置 `continue`，设置reason字段为你分析的依据。
3. 你在分析的时候，一定要结合下面给出来的上下文信息，进行严格的审查。特别是上下文中有关键信息的部分，一定要严格遵守。


**与问题相关的补充上下文信息**
{knowledge}

**输出格式：**
- 必须返回标准 JSON 字符串，确保可被 `json.loads()` 解析
- 包含两个个必要字段：`reason`、`conclusion`
- 不包含任何额外文本或解释
- 不要在外层添加额外的引号
- 确保是有效的JSON格式

**示例参考：**

当能够提供完整回答时的输出示例：
{terminate_fewshots}

当需要更多信息时的输出示例：
{continue_fewshots}


**注意：** 请严格遵循JSON格式输出，不要包含任何额外的解释或文本。

"""

# ==================== Codebase Indexer 代码搜索 Prompt ====================

SEARCH_CODE_SEGMENTS_PROMPT = """
# Role
你是一位精通代码架构分析的专家。你能够根据用户的问题，从代码分析结果中**精准定位**与问题**直接相关**的代码片段。

**当前时间**
{current_time}

# Task
基于提供的【代码分析结果】，根据用户的【分析需求】，识别出与用户问题**直接相关**的代码片段，并返回这些代码片段的位置信息。

# Step 1: 问题分类（Query Classification）
首先判断用户问题属于哪种类型：
- **精确查询**：问特定函数/方法的实现（如"xxx方法怎么实现的"）→ 只返回**1-3个**精确匹配的代码
- **实体查询**：问某个类/模型（如"Order类的结构"）→ 返回实体定义 + **核心业务方法**（不超过5个方法）
- **流程查询**：问业务流程（如"下单流程"、"支付流程"）→ 返回流程**完整调用链**上的所有关键节点

# Step 2: 相关性评分（Relevance Scoring）
为每个候选代码片段打分（1-10分）：
- **10分**：用户问题直接提到的代码（名称完全匹配）
- **8-9分**：实现用户问题核心逻辑的代码
- **7-8分**：流程中被调用的重要方法（如 check_xxx, validate_xxx, apply_xxx, process_xxx）
- **6-7分**：流程中的辅助操作（如 reserve_xxx, deduct_xxx, release_xxx）
- **5分以下**：间接相关或通用工具代码

**评分阈值**：
- 精确查询/实体查询：只返回评分 >= 7 的代码片段
- **流程查询：返回评分 >= 6 的代码片段**（确保流程完整性）

# Step 3: 选择标准（基于语义相关性）
- **必须选择**（评分9-10）：用户问题直接提到的代码
- **应该选择**（评分7-8）：
  - 实现用户问题核心逻辑的代码
  - 被核心代码**调用**的方法（向下追踪）
  - **调用**核心代码的方法（向上追踪，展示使用场景）
- **考虑选择**（评分6-7）：流程的后续处理、通知、状态变更等
- **不选择**（评分<6）：与业务意图无关的代码

# Step 4: 完整性检查（基于依赖关系）

**核心原则**：利用代码分析中的 `dependence` 字段追踪调用链

## 4.1 依赖链追踪
- 找到核心方法后，检查它**依赖**了哪些其他文件/方法（向下追踪）
- 检查哪些代码**依赖**了核心方法（向上追踪，找调用方）

## 4.2 层级覆盖检查
对于流程查询，确保覆盖了流程涉及的所有层级：
- 服务层（业务编排）
- 实体/模型层（数据和业务规则）
- 数据访问层（如果涉及持久化）

## 4.3 实体/类的方法完整性
当用户问某个实体或类时，返回该实体定义的**所有方法**，不要遗漏任何一个

# 数量控制原则
| 问题类型 | 建议返回数量 |
|---------|------------|
| 精确查询 | 1-3 个 |
| 实体查询 | 实体 + 该实体的全部方法 |
| 流程查询 | 根据流程复杂度，覆盖完整调用链 |

**原则**：宁可多返回相关代码，也不要遗漏关键节点

# Constraints
- **精准优先**：只返回与用户问题**直接相关**的代码，避免返回"可能相关"的代码
- **评分过滤**：只返回 relevance_score >= 7 的代码片段
- **行号准确**：必须提供准确的 line_no 信息
- **名称格式**：name 字段只填写函数/方法/类的名称，**不要带类名前缀**
  - 正确: "create_order", "validate_coupon", "Order"
  - 错误: "OrderService.create_order", "CouponService.validate_coupon"
- **输出要求**：仅返回标准 JSON，严禁任何 markdown 代码块标识符外的废话

# Output Format (JSON)
{{
  "query": "用户的原始问题",
  "query_type": "precise | entity | process",
  "intent_analysis": "对用户意图的分析，说明用户想了解什么",
  "relevant_segments": [
    {{
      "file_path": "文件路径",
      "segment_type": "entity | function | api_endpoint | global_function",
      "name": "代码元素名称（只填名称，不带类名前缀）",
      "line_no": "行号范围，如 100-160 或单行 100",
      "relevance_score": 7-10,
      "relevance_reason": "为什么这段代码与用户问题直接相关",
      "business_meaning": "该代码片段的业务含义"
    }}
  ],
  "summary": "对搜索结果的总结"
}}

---
# Context: 代码分析结果
{code_analysis}

---
# User Query: 分析需求

"""

ANSWER_WITH_CODE_PROMPT = """
# Role
你是一位精通代码分析的专家。你能够根据用户的问题和相关代码片段，提供准确、详细的回答。

**当前时间**
{current_time}

# Task
基于提供的【相关代码片段】，回答用户的【分析需求】。

# 相关代码片段
以下是与用户问题相关的代码，每个片段都包含：文件路径、代码类型、业务含义、相关性说明和实际代码内容。

{code_snippets}

# 用户问题
{query}

# Output Requirements
- 使用 Markdown 格式回答
- 直接回答用户的问题
- 引用具体的代码来支持回答
- 解释代码的业务逻辑和调用关系

"""

QUICK_RELEVANCE_CHECK_PROMPT = """
# Task
快速判断以下代码片段是否与用户查询相关。

# 用户查询
{query}

# 代码片段
文件: {filepath}
类型: {segment_type}
名称: {name}
描述: {description}

# 判断标准
- 相关：代码片段直接实现了用户查询的功能，或是该功能的核心依赖
- 不相关：代码片段只是名字相似，但功能与用户查询无关

# Output (JSON)
{{"relevant": true/false, "reason": "一句话说明"}}
"""

EXTRACT_KEYWORDS_PROMPT = """
# Role
你是一个代码搜索关键词提取专家。你的任务是从用户的查询中提取出用于代码搜索的**精确**关键词。

# Task
分析用户的查询，将关键词分为两类：
1. **主体关键词（entity）**：业务实体、模块、服务名称，如 订单、用户、支付、Order、User
2. **动作关键词（action）**：操作、动作、行为，如 创建、查询、删除、create、update

# 关键词提取策略
## 主体关键词（必须提取）
- 业务实体名：订单、用户、商品、支付
- 类/服务名：OrderService, PaymentGateway, User
- 精确标识符（如果是函数名）：create_order, processPayment

## 动作关键词（可选）
- 操作动词：创建、查询、更新、删除、处理、验证
- 英文动词：create, get, update, delete, process, verify

# 重要规则
- **主体关键词**是搜索的核心，用于定位相关文件/模块
- **动作关键词**用于在主体范围内进一步筛选
- 如果查询包含完整函数名（如 create_order），将其放入主体关键词，不要拆分
- **禁止**提取通用词：代码、函数、方法、实现、流程、逻辑
- 主体关键词**最多 3 个**，动作关键词**最多 2 个**

# 示例
- "创建订单的流程" → entity: ["订单", "Order"], action: ["创建", "create"]
- "create_order 函数" → entity: ["create_order"], action: []
- "用户登录验证" → entity: ["用户", "User"], action: ["登录", "验证"]
- "支付回调处理" → entity: ["支付", "Payment"], action: ["回调", "callback"]

# Output Format (JSON)
{{
    "entity_keywords": ["主体1", "主体2"],
    "action_keywords": ["动作1"],
    "reasoning": "简要说明"
}}

# 用户查询
{query}
"""

BATCH_SNIPPET_SCORE_PROMPT = """
# Task
用户提出了一个代码分析问题。下面是一批**完整的代码块**（每个 snippet_id 对应一个完整代码块，请勿拆分理解）。
请为**每一个** snippet_id 评估与用户问题的相关程度。

# 用户问题
{query}

# 代码块列表
{snippets_block}

# 评分标准（relevance_score 0-10）
- 9-10：直接实现用户问题的核心逻辑
- 7-8：强相关依赖、关键数据模型或 API
- 4-6：间接相关，可作上下文参考
- 0-3：import、main、无关工具类或噪声

# 输出要求
- 必须为列表中**每个** snippet_id 返回一条结果
- description：1-2 句话说明该代码块与用户问题的关系
- 仅返回标准 JSON，不要 markdown 代码块包裹

# Output Format (JSON)
{{
  "scores": [
    {{
      "snippet_id": 0,
      "relevance_score": 9.0,
      "description": "该代码块与用户问题的关系说明"
    }}
  ]
}}
"""
