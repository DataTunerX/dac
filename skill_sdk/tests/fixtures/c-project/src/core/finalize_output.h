/*
 * finalize_output.h — post-processing utility.
 *
 * hover on FinalizeOutput shows documentation comment.
 */
#ifndef FINALIZE_OUTPUT_H
#define FINALIZE_OUTPUT_H

char *finalize_output(const char *data);

#endif /* FINALIZE_OUTPUT_H */
