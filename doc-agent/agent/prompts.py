LOCATE_KNOWLEDGE_PROMPT_ZH = """

# Role
你是一位精通知识检索与信息定位的专家。你能根据用户问题的真实意图，从大量知识摘要中精准定位与问题相关的知识记录。

**当前时间**
{current_time}

# Task
基于提供的【知识摘要列表】，深入理解每条记录的核心内容。根据用户的【查询问题】，判断哪些知识记录可能包含回答问题所需的信息，返回这些记录的 Knowledge ID。

# 检索推理法则
在选择相关知识时，请遵循以下逻辑：
1. **意图理解**：理解用户问题的真实意图，而不仅仅是关键词匹配。
2. **相关性判断**：摘要中是否提及了与问题直接相关的概念、实体、流程或数据？
3. **宁多勿少**：如果不确定某条记录是否相关，倾向于将其包含进来，避免遗漏关键信息。
4. **排除无关**：明显与问题无关的记录（如完全不同的主题领域）应排除。

# Constraints
- **禁止猜测**：只基于摘要内容进行判断，不要假设摘要未提及的内容。
- **输出要求**：仅返回标准 JSON，严禁任何 markdown 代码块标识符或多余文字。

# Output Format (JSON)
{{
  "knowledge_ids": ["Knowledge ID 1", "Knowledge ID 2", "Knowledge ID 3"],
  "intent_analysis": "对用户真实意图的理解。",
  "reasoning": "选择这些知识记录的原因。"
}}

---
# Context: 知识摘要列表
{knowledge}

---
# User Query: 用户问题

"""

REQUERY_PROMPT_ZH = """
您是一位问题生成专家。您的任务是根据给定的信息生成5个类似的问题，然后从5个新生成的相似问题中选择一个问题，但是一定要注意新生成的问题不能和提供的历史问题中的重复了。


**当前时间**
{current_time}

**历史的执行结果**
{step_history}

**回答决策规则：**
1. 若新的问题可以正常生成出来，那就设置conclusion字段设置 `terminate`，设置requery字段为新生成的问题。
2. 若新的问题无法正常生成出来，那就设置conclusion字段设置 `continue`，设置requery字段为空的字符串。
3. 生成新问题的重要依据是历史的执行结果，你需要严格认真分析历史的执行结果，作为生成新问题的依据，来更好的解决问题。

**输出格式：**
- 必须返回标准 JSON 字符串，确保可被 `json.loads()` 解析
- 包含两个个必要字段：`requery`、`conclusion`
- 不包含任何额外文本或解释
- 不要在外层添加额外的引号
- 确保是有效的JSON格式

**示例参考：**

当能够提供完整回答时的输出示例：
{terminate_fewshots}

当需要更多信息时的输出示例：
{continue_fewshots}


**requery 生成示例参考：**
原始问题：农商银行总行2025年1月份的存款总额是多少？

新的类似问题：
1. 农商银行2025年1月的存款业务总体规模如何？
2. 农商银行总行在2025年1月底的存款总余额是多少？
3. 农商银行总行在2025年1月的存款规模达到了多少？
4. 农商银行总行2025年1月存了多少钱？
5. 农商银行总行截至2025年1月31日的存款总量数据？


**系统将根据原始问题 及历史查询记录 几个方面综合判断并生成相应输出。**
- 原始问题是： `{original_query}`
- 历史问题包括： `{history_querys}`


**注意：** 请严格遵循JSON格式输出，不要包含任何额外的解释或文本。

"""

OBSERVE_PROMPT_UNSTRUCTURED_ZH = """
您是一位问题解答专家。您的任务是根据问题和答案，来分析这个答案是不是和问题是相关的。不管是不是相关的，最后都要给出分析的依据。


**当前时间**
{current_time}


**回答决策规则：**
1. 如果当前的答案和问题是相关的，且答案中**包含具体事实、数据或事件**（如数字、时间、名称、过程等），则设置 conclusion 为 `terminate`，reason 为分析依据。
2. 若答案**仅是对背景知识的评价**（如仅写“当前背景知识提供了…能够充分解答用户的问题”），而**未包含任何具体事实、数据或事件**，应判定为不满足：设置 conclusion 为 `continue`，reason 中说明“答案需写出从知识中提取的具体内容，而非仅说明能解答”。
3. 若答案与问题不相关或无法正常回答，设置 conclusion 为 `continue`，reason 为你分析的依据。


**与问题相关的补充上下文信息**
{knowledge}

**输出格式：**
- 必须返回标准 JSON 字符串，确保可被 `json.loads()` 解析
- 包含两个个必要字段：`reason`、`conclusion`
- 不包含任何额外文本或解释
- 不要在外层添加额外的引号
- 确保是有效的JSON格式

**示例参考：**

当能够提供完整回答时的输出示例：
{terminate_fewshots}

当需要更多信息时的输出示例：
{continue_fewshots}


**注意：** 请严格遵循JSON格式输出，不要包含任何额外的解释或文本。

"""

NEXT_STEP_PROMPT_ZH = """
根据用户的问题和提供的背景知识，请遵循以下规则进行响应：


**当前时间**
{current_time}


**回答规则：**
1. 若背景知识能够充分解答用户问题，请提供完整回答并在结论字段返回 `terminate`。
2. 若背景知识与用户问题无关或信息不足，请不要直接回答问题，请根据原始的问题，保留原问题的语意，重新生成一个更清晰易懂的相似的新问题，放入 `requery` 字段, 你要重新生成问题就行。
3. 在生成问题的时候, 不要让用户补充材料。
4. 在生成问题的时候, 要仔细检查历史的query列表，不要生成和历史query重复或者相同的问题，生成出来5个相似的问题，然后从中选择一个和之前的历史query不同的问题，作为下次提问的问题。
5. 在 `answer` 字段中说明无法直接回答的原因，并提示需要更相关的信息。

**输出格式要求：**
- 必须返回标准的 JSON 格式字符串
- 确保输出可直接被 `json.loads()` 解析
- 包含三个必要字段：`answer`, `conclusion`, `requery`

**requery的示例**

原来的提问: python是什么?

新的相似的问题:

1. Python语言的基本概念和主要应用领域是什么？
2. 请介绍Python编程语言的特点和典型使用场景
3. Python是什么类型的语言？它主要用于哪些方面？
4. 能详细说明Python的定义和它的主要功能用途吗？
5. Python编程语言的核心特征和常见应用有哪些？
6. 解释Python的定位以及它在实际项目中的主要作用
7. Python语言的基本介绍和其主要应用范围
8. 什么是Python？它在软件开发中的主要用途是什么？
9. 请描述Python语言的性质和它最常被使用的领域
10. Python编程语言的基本概况和典型应用场景有哪些？


**原始的问题**

{original_query}

**历史的query列表:**

{history_querys}

**示例参考：**

可完整回答时的输出示例：
{terminate_fewshots}

需要更多信息时的输出示例：
{continue_fewshots}

**相关信息**
{memory}

**当前背景知识：**
{knowledge}

**注意：** 请严格遵循JSON格式输出，不要包含任何额外的解释或文本。

"""

BATCH_KNOWLEDGE_SCORE_PROMPT = """
# Task
用户提出了一个文档/知识问答问题。下面是一批**完整的知识块**（每个 block_id 对应一个完整知识块，请勿拆分理解）。
请为**每一个** block_id 评估与用户问题的相关程度。

# 用户问题
{query}

# 知识块列表
{knowledge_blocks}

# 评分标准（relevance_score 0-10）
- 9-10：直接包含回答用户问题所需的核心信息
- 7-8：强相关背景、流程说明或关键定义
- 4-6：间接相关，可作上下文参考
- 0-3：主题无关、重复或噪声内容

# 输出要求
- 必须为列表中**每个** block_id 返回一条结果
- description：1-2 句话说明该知识块与用户问题的关系
- 仅返回标准 JSON，不要 markdown 代码块包裹

# Output Format (JSON)
{{
  "scores": [
    {{
      "block_id": 0,
      "relevance_score": 9.0,
      "description": "该知识块与用户问题的关系说明"
    }}
  ]
}}
"""
