---
name: user_query
description: 查询用户静态本体：用户ID、用户名、电话、邮箱。数据在 data/users.txt，格式 用户ID|用户名|电话|邮箱。用 shell（cat/grep/awk）读 data/，不依赖外部 API。不掌握订单、商品名称、标价或库存。若问题涉及购买/订单，先查到用户ID，再交给 order_query，不要直接查商品。
---

# 用户查询能力

## 数据来源
用户数据存储在 `data/users.txt` 文件中，每行一条记录，字段以 `|` 分隔：
- 字段1：用户ID（如 U001）
- 字段2：用户名
- 字段3：电话
- 字段4：邮箱

## 输入 / 输出
- 输入：用户名、用户ID、电话或邮箱
- 输出：用户ID、用户名、电话、邮箱
- 没有：订单、商品ID、商品名称、标价、库存

## 数据查询方式
- 读取全部用户：`cat data/users.txt`
- 按用户ID查询：`grep "U001" data/users.txt`
- 按用户名查询：`grep "张三" data/users.txt`
- 按邮箱查询：`grep "zhangsan@example.com" data/users.txt`
- 提取用户名列表：`awk -F'|' '{print $2}' data/users.txt`

## 支持的查询场景
1. 根据用户ID查询用户详细信息
2. 根据用户名查询用户信息
3. 根据邮箱查找用户
4. 列出所有用户
5. 统计用户总数

## 跨域规划（强制）
- 本技能只解决「人是谁」。查到用户ID后，若用户还问买过什么/订单，委派 **order_query**，query 必须带上该用户ID。
- **禁止**直接委派商品查询：商品技能不能按用户名或用户ID过滤。没有订单里的商品ID之前，商品查询无法回答「某人买过什么」。
- 不要把订单或商品字段编进本技能的返回值。
