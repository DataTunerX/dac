# 测试 Skill 目录

本目录分两块：

1. **能力检查稳定性（10 个新业务域）**：`inventory-query`、`payment-query`、`expense-query`、`ticket-query`、`incident-query`、`hr-policy-qa`、`campaign-query`、`logistics-query`、`procurement-query`、`contract-query`。
2. **跨 SG 协作 A→B→C（原有）**：`user-query`、`order-query`、`product-query`，见下文专节，不计入这 10 个。

同一问句、同一技能正文多次评估时，`can_handle` / `can_contribute` 与 D 维排除项应稳定。正文按 I/D/O/R/C 写清字段或主题、输入输出、工具、只读/同步周期、**明确排除项**。

| 业务域 | 目录 | 技能名 | 类型 | 本域能回答 | 明确没有 |
|--------|------|--------|------|------------|----------|
| 仓储库存 | `inventory-query` | `inventory_query` | 结构化 | 商品ID/仓库ID → 各仓库库存、安全阈值 | 商品名称、标价、运单、采购在途 |
| 支付退款 | `payment-query` | `payment_query` | 结构化 | 支付单号/订单号/用户ID → 金额、方式、状态、退款额 | 用户名、商品、物流、汇率 |
| 财务报销 | `expense-query` | `expense_query` | 结构化 | 工号/报销单号 → 费用类型、金额、审批状态 | 姓名、工资薪酬、报销制度原文 |
| 客服工单 | `ticket-query` | `ticket_query` | 结构化 | 工单号/用户ID → 类型、状态、处理人、摘要 | 用户名、物流轨迹、知识库全文 |
| IT 运维 | `incident-query` | `incident_query` | 结构化 | 服务名/告警ID → 级别、指标、CPU/错误率 | 业务订单/GMV、慢 SQL 原文、安全入侵 |
| HR 制度 | `hr-policy-qa` | `hr_policy_qa` | 非结构化 | 年假/考勤/差旅标准/入职离职流程 | 薪酬期权绩效；劳动法条；个人假期余额 |
| 营销活动 | `campaign-query` | `campaign_query` | 结构化 | 活动ID/渠道 → 预算、消耗、券批次与核销 | 订单GMV、商品标价库存、用户姓名、广告素材 |
| 物流履约 | `logistics-query` | `logistics_query` | 结构化 | 运单号/订单号 → 承运商、状态、当前站点、签收时间 | 用户名、库存、支付、采购在途 |
| 采购订单 | `procurement-query` | `procurement_query` | 结构化 | 采购单号/供应商/物料ID → 数量、进货价、交期、在途 | 零售标价、仓库现存量、运单轨迹、合同正文 |
| 合同台账 | `contract-query` | `contract_query` | 结构化 | 合同号/对方/负责人工号 → 金额、期限、状态 | 合同条款原文、劳动法、报销单、采购行项目 |

Join 键：库存/采购用 `商品ID(PROD-xxx)`、`仓库ID(WH-xxx)`；支付/物流/工单用 `订单号(ORD-xxx)` 或 `用户ID(U00x)`；报销/合同用 `员工工号(EMP-xxx)`（**不等于**用户ID）；营销用 `活动ID(CMP-xxx)` / `券批次(CPN-xxx)`。

---

# A → B → C，且 B 走 2 轮

用本目录**原有**三个 skill 跑跨 SG 协作时，目标拓扑如下。

| 角色 | Agent（建议名） | Skill | 本域能回答 | 本域没有 |
|------|-----------------|-------|------------|----------|
| A | `user-agent` | `user-query` | 姓名 → 用户ID | 订单、商品 |
| B | `order-agent` | `order-query` | 用户ID → 订单号、商品ID | 商品名称、标价、库存 |
| C | `product-agent` | `product-query` | 商品ID → 名称、标价、库存 | 用户、订单 |

Join key：`用户名 → 用户ID → 商品ID`。C 不能按姓名或用户ID查，所以必须先经过 B。

## 推荐问句

```
张三买过哪些商品？请给出商品名称和现在的官方标价。
```

张三在数据里是 `U001`，订单对应 `PROD-001`（已发货/运输中）/ `PROD-003`（已完成/已签收）/ `PROD-016`（已发货/物流异常）。

## 期望执行

1. **A 一轮**：本地 `grep 张三` 得到 `U001`，本域无法回答「买过什么/标价」。按 skill 规则只委派 B，query 带上 `U001` 以及仍缺的名称和标价。**不要**直接委派 C（没有商品ID时 C 查不了「张三」）。
2. **B 第 1 轮**：只有用户ID，按 skill 规则**只跑本地订单查询**，得到商品ID，不要在这一轮委派 C。评估：用户要名称和标价，本轮只有 ID → 不通过。
3. **B 第 2 轮**：上下文里已有 `PROD-001` 等，委派 C，把这些商品ID写进 query。
4. **C 一轮**：按商品ID返回名称和标价。

B 所在 agent 需要 `max_loops >= 2`。若 B 第一轮就把 product 写进同一 plan，会变成单轮内本地+委派，**不是**本 case 要的两轮。

## 为什么 skill 要这样写

- A 的 skill **点名**订单、**禁止**在没有商品ID时找商品 → 链路是 A→B，不是 A 同时呼 B 和 C。
- B 的 skill **诚实**（只有商品ID），并写明「还没有商品ID时不要委派商品查询」→ 压成两轮，而不是 chain planner 一轮三步。
- C 的 skill **只收商品ID** → 倒逼必须先跑 B。

---

# 能力检查稳定性推荐问句（仅 10 个新域）

同一问句应对同一 skill 稳定给出下表结论。跨域问句不要因为关键词碰巧出现（如 `order-svc` 含 order）就判本域可独立完成。

## 应独立处理（can_handle=true）

| ID | 问句 | 期望 skill |
|----|------|------------|
| H01 | 查询商品 PROD-001 在各仓库的库存量，哪些低于安全阈值 | `inventory_query` |
| H02 | 查询支付单 PAY-001 的金额、方式和退款金额 | `payment_query` |
| H03 | 列出 EMP-001 的报销单并汇总金额 | `expense_query` |
| H04 | 查询用户 U001 目前处理中的工单 | `ticket_query` |
| H05 | order-svc 最近的 P1 告警是什么？错误率和 CPU 是多少 | `incident_query` |
| H06 | 公司的年假申请流程是什么？需要提前多少天申请 | `hr_policy_qa` |
| H07 | 查询活动 CMP-001 的预算、已消耗和状态 | `campaign_query` |
| H08 | 查询运单 SHP-001 的承运商、当前站点和物流状态 | `logistics_query` |
| H09 | 华储电子还有哪些在途采购？PROD-001 未到货数量是多少 | `procurement_query` |
| H10 | 查询合同 CON-001 的金额、到期日和状态 | `contract_query` |

## 应拒绝（can_handle=false，can_contribute=false，D=0 solid）

| ID | 问句 | 问的是 | 不要误命中 |
|----|------|--------|------------|
| R01 | 公司辞退员工的法定补偿标准是什么？工作5年月薪12000 | 劳动法 | `hr_policy_qa` / `contract_query` / 其余 8 个 |
| R02 | 企业所得税汇算清缴的流程和截止日期 | 税务 | 全部 10 个 |
| R03 | 糖尿病患者每天糖分摄入应控制多少克 | 医疗 | 全部 10 个 |
| R04 | 把这段英文翻译成中文 The quick brown fox | 翻译 | 全部 10 个 |
| R05 | 北京明天天气怎么样 | 天气 | 全部 10 个 |
| R06 | 公司今年的薪酬调整方案和期权行权条件 | 薪酬期权 | `hr_policy_qa`（正文排除） |
| R07 | MySQL 的订单表应该怎么设计索引 | 库表设计 | `incident_query` / `payment_query` |
| R08 | 当前 CNY 到 USD 汇率 | 外汇 | `payment_query` |

## 可贡献、不能独立完成（can_handle=false，can_contribute=true）

| ID | 问句 | 贡献者 | 缺什么 |
|----|------|--------|--------|
| C01 | ORD-001 物流到哪了，支付成功了没有 | `logistics_query`（轨迹） | 支付状态 |
| C02 | ORD-001 物流到哪了，支付成功了没有 | `payment_query`（支付） | 运单轨迹 |
| C03 | PROD-001 各仓库现存量，以及未到货采购量 | `inventory_query`（现存量） | 采购在途 |
| C04 | PROD-001 各仓库现存量，以及未到货采购量 | `procurement_query`（在途） | 仓库现存量 |
| C05 | TKT-002 这张物流咨询工单，对应运单到哪了 | `ticket_query`（摘要里有 ORD-005） | 轨迹字段 |
| C06 | TKT-002 这张物流咨询工单，对应运单到哪了 | `logistics_query`（需订单号/运单号） | 工单本身 |
| C07 | EMP-001 报销合计，以及他名下履行中合同金额 | `expense_query` / `contract_query` | 对方缺的字段 |
| C08 | CMP-001 开学季活动卖了多少 GMV，预算花了多少 | `campaign_query`（预算/消耗） | 订单 GMV |
| C09 | 华储电子的采购合同金额，和对该供应商的在途采购数量 | `contract_query`（台账金额） / `procurement_query`（采购行） | 对方缺的粒度 |

## 同领域但操作不支持（O=0，只读技能）

| ID | 问句 | skill |
|----|------|-------|
| O01 | 给 PAY-001 发起退款 | `payment_query` |
| O02 | 把 PROD-001 在华东仓的库存改成 0 | `inventory_query` |
| O03 | 关闭告警 INC-001 并重启 order-svc | `incident_query` |
| O04 | 帮我在 OA 提交年假申请 | `hr_policy_qa` |
| O05 | 把 CMP-001 预算追加 10 万并开始发券 | `campaign_query` |
| O06 | 把运单 SHP-001 标记为已签收 | `logistics_query` |
| O07 | 确认 PO-001 整单收货入库 | `procurement_query` |
| O08 | 终止合同 CON-001 并提交用印 | `contract_query` |

## 非结构化推荐问句（应拒绝：10 个新域均无劳动法 skill）

1. 公司长期拖欠加班费，我想离职并要求经济补偿。
2. 法定节假日和休息日的加班费分别应该怎么算？
3. 我能以拖欠工资为由解除合同吗？如果能，工作5年、月薪12000，经济补偿该怎么算？

以上三句应对这 10 个 skill 稳定 `can_handle=false`（`hr_policy_qa` / `contract_query` 正文均不覆盖法定补偿与加班费倍率）。

---

# 各 skill 常见提问（每域 10 条）

均为本域可独立处理的问句（期望 `can_handle=true`），覆盖单条查询、条件筛选、汇总统计。ID 与 `data/` 中记录一致。

## inventory-query 仓储库存

1. PROD-001 在哪些仓库有货？各仓当前库存是多少？
2. 华东仓现在都有哪些商品，库存分别是多少？
3. 哪些商品已经低于安全库存阈值？
4. 华北仓 PROD-016 现在还剩多少？有没有低于安全线？
5. 华南仓的库存总量是多少？
6. PROD-003 最近一次出库是什么时候？
7. 列出库存量少于 20 的全部记录。
8. WH-002 里库存最低的是哪个商品？
9. PROD-001 三个仓加起来一共有多少件？
10. 华东仓有没有库存低于安全阈值的货？

## payment-query 支付退款

1. 支付单 PAY-001 的金额、支付方式和状态是什么？
2. 订单 ORD-004 有没有退款？退了多少？
3. 用户 U001 的支付流水列一下，一共成功支付了多少钱？
4. 目前有哪些待支付的记录？
5. 用微信支付成功的一共有多少笔、总额多少？
6. 所有已退款记录的退款金额合计是多少？
7. PAY-012 为什么失败了？支付方式和时间是什么？
8. 用户 U004 有没有正在退款中的支付单？
9. 9 月以来成功支付的总金额是多少？
10. 按支付方式汇总成功交易额。

## expense-query 财务报销

1. 报销单 EXP-001 的费用类型、金额和审批状态？
2. EMP-001 名下有哪些报销单，合计多少钱？
3. 现在还有哪些审批中的报销？
4. 本月差旅交通类报销一共多少笔、多少钱？
5. EMP-002 被驳回的报销是哪几单？
6. 发票号 INV-20260821-008 对应哪张报销单？
7. 已打款的报销单列出来，总额是多少？
8. EMP-005 的草稿和已驳回报销分别是什么？
9. 招待费目前有哪些单子还没结束？
10. 8 月份已通过的报销合计金额是多少？

## ticket-query 客服工单

1. 工单 TKT-001 现在谁在处理、状态是什么？
2. 用户 U001 有哪些还没关掉的工单？
3. 列出所有处理中的售后工单。
4. 客服-小陈手头待处理和处理中的单子有哪些？
5. 紧急优先级的工单现在有几张？分别是什么问题？
6. 最近的发票类工单都解决了没有？
7. TKT-006 的摘要是什么，派给谁了？
8. 按工单类型统计当前单量。
9. 待用户回复的工单有哪些？
10. 主管王名下的投诉单进展如何？

## incident-query IT 运维

1. 告警 INC-001 的级别、指标和当前状态？
2. order-svc 最近有哪些告警？还有没有未恢复的？
3. 现在触发中或处理中的告警列一下。
4. payment-svc 当前错误率和 CPU 大概什么水平？
5. redis-cache 内存是不是已经超 90%？
6. 今天上午 P1 告警有哪些，谁在跟？
7. mysql-primary 最近一次慢查询告警恢复了没有？
8. 按服务统计告警条数。
9. cdn-pop 现在的错误率是多少？
10. order-svc 从 10:00 到 10:05 CPU 和错误率是怎么变化的？

## hr-policy-qa HR 制度

1. 年假要提前几天在 OA 申请？一次请 5 天以上还要谁加签？
2. 入职满 3 年，每年有几天年假？当年没用完能结转到什么时候？
3. 迟到 10 分钟怎么处理？超过 30 分钟呢？
4. 出差去上海，普通员工住宿标准是多少？要带哪些报销材料？
5. 招待费报销的审批要过几级？
6. 试用期离职需要提前几天通知？转正后呢？
7. 餐补多少钱一天？迟到当天还有没有餐补？
8. 司龄一年的员工请病假，工资按什么比例发？要补什么证明？
9. 工作日加班没提前申请，能不能算调休？调休多久有效？
10. 婚假、陪产假各有多少天？有没有入职时长限制？

## campaign-query 营销活动

1. 活动 CMP-001 的预算、已消耗和当前状态？
2. 现在有哪些进行中的微信渠道活动？
3. 券 CPN-A09 的规则是什么，发放量和核销量分别多少？
4. 电子品类正在投放的活动有哪些？
5. 已暂停的活动列一下，各自花了多少预算？
6. 中秋礼盒专场预算花完了没有？核销率怎么样？
7. 国庆预热满减什么时候开始，券规则是什么？
8. 全部进行中活动的预算和消耗分别合计多少？
9. 站内渠道有哪些活动，状态分别是什么？
10. CPN-F06 包邮券发了多少、核了多少？

## logistics-query 物流履约

1. 运单 SHP-001 现在到哪了，什么状态？
2. 订单 ORD-005 的物流是哪家承运商，有没有开始派送？
3. 哪些运单还没签收？
4. 异常件有哪些，滞留在哪个站点？
5. 顺丰承运的运单目前都什么状态？
6. ORD-016 为什么显示异常？
7. 今天处于派送中的包裹有哪些？
8. 已签收的运单签收时间分别是什么时候？
9. 按承运商统计运单数量。
10. ORD-010 揽收了没有，现在在哪个站点？

## procurement-query 采购订单

1. 采购单 PO-001 的供应商、数量、交期和状态？
2. 华储电子还有哪些在途采购？
3. PROD-001 未到货数量是多少，准备进哪个仓？
4. 华东仓（WH-001）名下有哪些未入库采购单？
5. 列出所有已下单但还没在途的采购。
6. 声谷配件的采购单里，哪些还是草稿或在途？
7. MAT-201 最近一次入库是哪张采购单、多少数量？
8. 在途采购按仓库汇总数量。
9. PO-008 为什么还没下单？计划和单价是多少？
10. 华北光电名下采购金额合计多少？

## contract-query 合同台账

1. 合同 CON-001 的金额、到期日和履行状态？
2. EMP-001 名下履行中的合同有哪些、总额多少？
3. 和华储电子相关的合同列一下，有没有已终止的？
4. 今年 12 月 31 日前到期的合同有哪些？
5. 租赁类合同目前还有在履行的吗？
6. 服务类合同金额合计多少？
7. CON-010 还是草稿吗，负责人是谁？
8. EMP-003 负责的物业租赁合同什么时候到期？
9. 已到期或已终止的合同清单。
10. 采购类履行中合同，按金额从高到低排一下。

---

# 跨 skill 协作提问（20 条）

单 skill 答不完，需要 2～4 个 skill 接力。Join 键均能在 `data/` 对上。任一参与 skill 单独评估应为 `can_handle=false`、`can_contribute=true`。

| ID | 问句 | 协作 skill | Join 键 / 数据要点 |
|----|------|------------|-------------------|
| X01 | 订单 ORD-001 的钱付清了没有？货现在到哪个转运中心了？ | `payment_query` → `logistics_query` | ORD-001：PAY-001 微信成功 7999；SHP-001 顺丰运输中、杭州转运中心 |
| X02 | ORD-016 已经显示支付成功，为什么运单还是异常？滞留在哪个站点？ | `payment_query` → `logistics_query` | ORD-016：PAY-014 支付宝成功 5999；SHP-008 中通异常、杭州滞留 |
| X03 | PROD-001 三个仓库现存量分别多少？未到货采购还有多少、进哪个仓？ | `inventory_query` + `procurement_query` | 库存 86/40/55；PO-001 华储在途 200 件进 WH-001 |
| X04 | 华北仓的 PROD-016 已经低于安全库存，对应在途采购什么时候交货、供应商是谁？ | `inventory_query` → `procurement_query` | WH-002：18&lt;30；PO-004 华储 120 件、交期 2026-09-22、进 WH-002 |
| X05 | 工单 TKT-002 用户在问预计送达，对应运单到哪了、是哪家快递？ | `ticket_query` → `logistics_query` | 摘要含 ORD-005；SHP-003 顺丰派送中、北京朝阳网点 |
| X06 | 用户 U001 有哪些还没关闭的工单？他最近几笔支付分别是什么状态？ | `ticket_query` + `payment_query` | TKT-001/TKT-004 处理中；PAY-001/003 成功，PAY-014 成功 |
| X07 | EMP-001 那笔住宿报销 1680 块，按公司差旅标准算不算超标？一线城市住宿上限是多少？ | `expense_query` + `hr_policy_qa` | EXP-003 住宿 1680 已通过；制度一线城市普通员工 500 元/晚 |
| X08 | EMP-001 报销一共花了多少？他名下履行中的合同总额是多少？ | `expense_query` + `contract_query` | 报销 EXP-001/003/007；合同 CON-001/003/007 履行中 |
| X09 | 华储电子的采购合同金额和到期日是什么？对这家供应商现在还有哪些在途采购？ | `contract_query` + `procurement_query` | CON-001 124万至 2027-02-28；PO-001/004/009 在途 |
| X10 | 华东仓低于安全阈值的商品，有没有已经下单或在途的采购能补货？ | `inventory_query` → `procurement_query` | WH-001：PROD-002 12&lt;20、PROD-017 9&lt;12；PO-002/PO-012 已下单进 WH-001 |
| X11 | 用户 U008 那笔已退款支付退了多少钱？他的投诉工单现在处理到哪了？ | `payment_query` + `ticket_query` | PAY-013 已退款 149；TKT-009 投诉、待用户回复 |
| X12 | EMP-003 负责的租赁合同何时到期？他名下报销单审批到哪了？ | `contract_query` + `expense_query` | CON-002 履行中至 2026-12-31；EXP-004 已打款、EXP-009 审批中 |
| X13 | EMP-001 去出差的住宿和交通报销明细是什么？制度上住宿标准和必须提交的材料有哪些？ | `expense_query` + `hr_policy_qa` | EXP-001 差旅交通 1280.5、EXP-003 住宿 1680；`reimbursement.txt` |
| X14 | 工单 TKT-002 问的那单，用户付过款没有、快递有没有开始派送？ | `ticket_query` → `payment_query` + `logistics_query` | ORD-005：PAY-005 微信成功 6299；SHP-003 派送中 |
| X15 | 华南仓 PROD-011 库存很低，在途采购是哪家供应商、进哪个仓？和这家供应商的采购合同还在履行吗？ | `inventory_query` → `procurement_query` → `contract_query` | 库存 8&lt;15；PO-009 华储 25 件进 WH-003；CON-001 履行中 |
| X16 | 华储电子在途的 PROD-001 和 PROD-016 到仓后，对应仓库存量大概能到多少？ | `procurement_query` + `inventory_query` | PO-001：WH-001 86+200；PO-004：WH-002 18+120 |
| X17 | 张三的包裹现在到哪了？哪些已经签收、哪票异常？ | `user_query` → `order_query` → `logistics_query` | 张三=U001；ORD-001 运输中、ORD-003 已签收、ORD-016 杭州滞留 |
| X18 | 张三付过哪些款、有没有退款？对应包裹签收了没有？ | `user_query` → `payment_query` + `logistics_query` | U001 三笔均成功无退款；ORD-001 未签收、ORD-003 已签收、ORD-016 异常 |
| X19 | 张三买过哪些商品？请给出商品名称、现在的官方标价，以及各仓库还剩多少。 | `user_query` → `order_query` → `product_query` + `inventory_query` | PROD-001/003/016；标价 7999/1899/5999 |
| X20 | PROD-001 现在官方标价多少，各仓库库存多少，未到货采购还有多少？ | `product_query` + `inventory_query` + `procurement_query` | 标价 7999；库存三仓；PO-001 在途 200 |

X01–X16 只在 10 个新域之间协作。X17–X20 带上原有 `user-query` / `order-query` / `product-query`，覆盖「姓名 → 订单 → 履约/库存」链路。

